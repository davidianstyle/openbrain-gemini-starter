description = "Create an organization note with key people and places"
prompt = "Run the log-organization skill. Create a new organization note at + Atlas/Organizations/ from the Organization template. {{args}}"
